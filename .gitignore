# Go
daemon/knobd
daemon/*.test
daemon/coverage.out
/dist/

# Node / Vite
ui/node_modules/
ui/dist/
ui/.vite/
ui/*.tsbuildinfo

# Tauri / Rust
ui/src-tauri/target/
ui/src-tauri/Cargo.lock.bak
# Auto-generated by `tauri dev`/`tauri build` from capabilities/*.json;
# regenerated on every build, not something to hand-edit or diff.
ui/src-tauri/gen/

# Generated, but committed and drift-checked in CI (regenerate all three
# via `make schema`; ui/'s TypeScript types via `npm run codegen`):
#   docs/config.schema.json
#   docs/openapi.json
#   docs/device-layout.json

# makepkg output (packaging/arch)
packaging/arch/src/
packaging/arch/pkg/
packaging/arch/knobd/
packaging/arch/*.pkg.tar.*
packaging/arch/*.log

# Editors / OS
.DS_Store
*.swp
.vscode/
.idea/

# Local runtime artifacts, never real user config in-repo
*.local.json
