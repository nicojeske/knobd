# Go
daemon/knobd
daemon/*.test
daemon/coverage.out
/dist/

# Node / Vite
ui/node_modules/
ui/dist/
ui/.vite/
ui/*.tsbuildinfo

# Tauri / Rust
ui/src-tauri/target/
ui/src-tauri/Cargo.lock.bak

# Generated, but committed and drift-checked in CI:
#   docs/config.schema.json (regenerate via `make schema`)
#   ui/src/types/config.ts  (regenerate via `npm run codegen` in ui/)
# docs/openapi.json is generated too, but stays ignored until M07 gives
# daemon/internal/api something to describe.
docs/openapi.json

# Editors / OS
.DS_Store
*.swp
.vscode/
.idea/

# Local runtime artifacts, never real user config in-repo
*.local.json
